// Define the names
const A = "John";
const B = "Eliana";

// Construct the message using template literals
const message = 'A says Hi to B';

// Print the message to the console
console.log(message);